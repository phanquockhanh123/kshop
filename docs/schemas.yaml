Page:
  type: integer
  minimum: 1
  example: 1

Limit:
  type: integer
  minimum: 1
  maximum: 100
  example: 20

TotalPage:
  type: integer
  minimum: 1
  example: 5

TotalCount:
  type: integer
  minimum: 0
  example: 100

Token:
  type: string
  maxLength: 255
  example: "XXXXXXXXXXXXXXX"

GrantType:
  type: string
  enum: ["password", "refreshToken"]
  maxLength: 255
  example: "password"

Email:
  type: string
  maxLength: 256
  example: "test@gmail.com"

Password:
  type: string
  format: password
  maxLength: 20
  example: "Password"

Id:
  type: integer
  format: int64
  example: 1

Boolean:
  type: boolean
  example: true

Datetime:
  type: string
  maxLength: 25
  example: "2000-01-01 00:00:00"
